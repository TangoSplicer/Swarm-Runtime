[package]
name = "shard-worker"
version = "0.27.0"
edition = "2021"

# CRITICAL: This tag isolates the worker from the root workspace
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
# No external dependencies
