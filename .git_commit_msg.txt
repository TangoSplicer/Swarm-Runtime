feat(runtime): implement Phase 8 P2P Pre-Flight State Synchronization

- Gateway Routing: Upgraded scheduler to detect stateful WebAssembly payloads and dynamically route atomic, un-sharded invocations to multiple peers.
- Global State Tracker: Gateway now tracks the latest verified `.state` hash for every smart contract and attaches it to outbound job payloads.
- P2P State Sync: Workers now parse expected state hashes before execution. If local state is missing or outdated, workers dynamically fetch the 1MB memory snapshot from connected peers via the Kademlia DHT before executing.
- Auto-Scaling Redundancy: Gateway dynamically scales redundancy factors based on live peer availability to prevent SLA deadlocks.
